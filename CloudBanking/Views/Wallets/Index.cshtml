@model IEnumerable<CloudBanking.Models.Wallet>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_dashboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AuthKey)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WalletType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BankTransaction.TransactionType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AuthKey)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WalletType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BankTransaction.TransactionType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WalletID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WalletID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WalletID })
        </td>
    </tr>
}

</table>
